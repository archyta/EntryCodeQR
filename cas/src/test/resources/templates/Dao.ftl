package ${dao.packageName};

import tk.mybatis.mapper.common.Mapper;
import ${model.packageName}.${dao.modelName};

/**
 * ${tableComment}DAO接口
 */
public interface ${dao.modelName}Mapper extends  Mapper<${dao.modelName}> {
	
}
