<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.lanyue.cas.mapper.SysDictionaryMapper">

	<sql id="sysDictionaryColumnsNotA">
		id as id,
		code as code,
		name as name,
		parent_code as parentCode,
		key as key,
		value as value,
		description as description,
		order_num as orderNum,
		is_lock as isLock,
		DEL_FLAG as delFlag,
		CRT_BY as crtBy,
		CRT_TIME as crtTime,
		UPD_BY as updBy,
		UPD_TIME as updTime
	</sql>

</mapper>